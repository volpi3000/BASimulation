package main;

public class Enums {

	public enum oType {STREET, INTERSECTION,PARKINGSPOT,ENTRANCE, DUMMY}
	public enum direction{ NORTH, EAST, SOUTH, WEST}
	public enum status{ ONROUTE, SEARCHING, PARKED, ENTERING}
	public enum orientation{ H, V}
	public enum search{ NATURAL, APP}
	
	
}
